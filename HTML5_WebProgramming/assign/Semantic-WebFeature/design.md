### 학문적 본질 (이론):
- 브라우저 렌더링 원리: DOM 트리와 CSSOM 트리가 어떻게 결합되어 렌더 트리(Render Tree)를 만드는가?

- 레이아웃 알고리즘: 브라우저가 Reflow(배치 계산)와 Repaint(색칠)를 어떤 방식으로 최적화하여 수행하는가?

- 컴퓨터 그래픽스(Canvas): 픽셀이 화면에 찍히는 래스터화(Rasterization) 알고리즘은 무엇이고, 벡터 연산은 어떻게 처리되는가?